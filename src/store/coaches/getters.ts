export default {
    coachesList(state: any) {
        return state.coachesList
    },
    coachDetails(state : any, id : string){
        // return state.coachList.find(coach  => coach.id === id)
    }

}